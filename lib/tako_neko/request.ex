defmodule TakoNeko.Request do
  # alias TakoNeko.Client

  # @default_headers %{
  #   "accept" => "application/vnd.github+json",
  #   "x-github-api-version" => "2022-11-28"
  # }

  # def new(%Client{auth: auth} = client, url) do
  #   Req.new()
  # end

  # def new(%Client{} = client, :get, resource) do
  # end

  # def prepare(%Client{} = client, %__MODULE__{} = request) do

  # end
end
